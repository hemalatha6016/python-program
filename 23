#Goal: Compute the average height using only distinct values (no duplicates).

def average(array):
    unique_heights = set(array)
    return sum(unique_heights) / len(unique_heights)

if __name__ == '__main__':
    n = int(input())
    arr = list(map(int, input().split()))
    result = average(arr)
    print(result)
